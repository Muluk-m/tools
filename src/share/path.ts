import path from 'path'

export const DIR_PATH = path.resolve(__dirname, '../..')

export const TMP_PATH = path.resolve(DIR_PATH, '.tmp')

export const SOURCE_MAP_PATH = path.resolve(DIR_PATH, '.sourcemap')
